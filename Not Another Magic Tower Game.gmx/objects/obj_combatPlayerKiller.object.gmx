<!--This Document is generated by GameMaker, if you edit it by hand then you do so at your own risk!-->
<object>
  <spriteName>&lt;undefined&gt;</spriteName>
  <solid>0</solid>
  <visible>-1</visible>
  <depth>0</depth>
  <persistent>0</persistent>
  <parentName>&lt;undefined&gt;</parentName>
  <maskName>&lt;undefined&gt;</maskName>
  <events>
    <event eventtype="0" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>hit = false;
hp = 0;
sndMagic = false;
dmg = 0;
tempDmp = 0;
msg = "";
cooldown = 0;



</string>
          </argument>
        </arguments>
      </action>
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>/// Added (some common variables used on their child objects)
status = 0;
phase = 0;
time = 0;
dir = 0;
dis = 0;
targetX = 0;
targetY = 0;
spd = 0;
type = 0;
start = 0;

angle = 0;
alpha = 0;
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="2" enumb="9">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>player.image_alpha = 1;
player2.image_alpha = 1;
global.cooldown = false;
hit = false;
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="2" enumb="8">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>if(global.cooldown)
{
    with(player)
    {
        if(image_alpha == 0)
        {
            image_alpha = 1;
        }
        else 
        {
            image_alpha = 0;
        }
    }
    with(player2)
    {
        if(image_alpha == 0)
        {
            image_alpha = 1;
        }
        else 
        {
            image_alpha = 0;
        }
    }
    alarm[8] = 5;
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="4" ename="obj_playerDotMask">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>if(instance_exists(obj_tlRngBlackOut) || instance_exists(obj_tlRngBlackIn)) exit;

if(!global.cooldown &amp;&amp; !global.godmod)
{   
    sndMagic = false;
    //hit = true;
    if(global.defenseDamage &lt; 1)
    {
        global.defenseDamage = 1;
    }
    if(global.GalleryBossFight)
    {
        global.monsterSuperCritical = false;
        global.monsterCritical = false;
    }
    if(!global.Blockable)
    {
        if(global.monsterSuperCritical)
        {
            dmg = round(global.defenseDamage * 2.25);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak &amp;&amp; global.MagicHP &gt; 0)
                    {                    
                        if(global.MagicHP &gt;= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
            global.monsterSuperCritical = false;
        }
        else if(global.monsterCritical)
        {
            dmg = round(global.defenseDamage * 1.5);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak &amp;&amp; global.MagicHP &gt; 0)
                    {                    
                        if(global.MagicHP &gt;= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
            global.monsterCritical = false;
        }
        else
        {
            dmg = global.defenseDamage;
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= global.defenseDamage;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak &amp;&amp; global.MagicHP &gt; 0)
                    {                    
                        if(global.MagicHP &gt;= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "Kid got " + string(dmg) + " damage.";
            }
        }  
        with(player)
        {
            damageInfo = instance_create(x + (sprite_width / 2 - sprite_xoffset) * image_xscale,y + (sprite_height / 2 - sprite_yoffset) * image_yscale,obj_damageInfo);
            damageInfo.damage = other.dmg;
            
            if(global.monsterSuperCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 2.25);
                damageInfo.font = fontDamageInfo3;
                damageInfo.color = c_red;        
                damageInfo.level = 3;
            }
            else if(global.monsterCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 1.5);
                damageInfo.font = fontDamageInfo2;
                damageInfo.color = c_yellow;        
                damageInfo.level = 2;
            }
            else
            {
                //damageInfo.damage = global.defenseDamage;
                damageInfo.font = fontDamageInfo1;
                damageInfo.color = c_white;        
                damageInfo.level = 1;
            }
        }
    }
    if(global.hp &lt;= 0 &amp;&amp; !global.GalleryBossFight)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(player)
        {
            killPlayer();
        }
    }
    else if(global.GalleryBossFight &amp;&amp; global.GalleryBossFightHp &lt;= 0)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(player)
        {
            killPlayer();
        }
    }
    else
    {
        global.cooldown = true;
        if(global.Blockable)
        {
            if(global.isAddMsg)
            {
                msg = "Kid block the damage.";
            }
            global.Blockable = false;
            sound_fix(sndgras);
            instance_create(player.x, player.y, obj_blockBubble);
            global.monsterCritical = false;
            global.monsterSuperCritical = false;
            /*if(object_index != global.combatObj &amp;&amp; !isStable)
            {                
                instance_destroy();
            }*/
        }
        else
        {
            if(sndMagic)
            {
                sound_fix(sndWind2);
                with(instance_create(x,y,obj_magicBlock))
                {
                    target = player;
                }
            }
            else
            {
                sound_fix(sndKidHit);
                obj_playerHitController.alarm[8] = 1;
            }
        }    
        obj_playerHitController.alarm[9] = 50;
    }
    if(global.isAddMsg)
    {
        with(global.msgController)
        {
            msg = other.msg;
            event_user(0);
        }
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="4" ename="obj_phase4BounceKid">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>if(instance_exists(obj_tlRngBlackOut) || instance_exists(obj_tlRngBlackIn)) exit;

if(!global.cooldown &amp;&amp; !global.godmod)
{   
    sndMagic = false;
    //hit = true;
    if(global.defenseDamage &lt; 1)
    {
        global.defenseDamage = 1;
    }
    if(global.GalleryBossFight)
    {
        global.monsterSuperCritical = false;
        global.monsterCritical = false;
    }
    if(!global.Blockable)
    {
        if(global.monsterSuperCritical)
        {
            dmg = round(global.defenseDamage * 2.25);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak &amp;&amp; global.MagicHP &gt; 0)
                    {                    
                        if(global.MagicHP &gt;= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
            global.monsterSuperCritical = false;
        }
        else if(global.monsterCritical)
        {
            dmg = round(global.defenseDamage * 1.5);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak &amp;&amp; global.MagicHP &gt; 0)
                    {                    
                        if(global.MagicHP &gt;= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
            global.monsterCritical = false;
        }
        else
        {
            dmg = global.defenseDamage;
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= global.defenseDamage;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak &amp;&amp; global.MagicHP &gt; 0)
                    {                    
                        if(global.MagicHP &gt;= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "Kid got " + string(dmg) + " damage.";
            }
        }  
        with(obj_phase4BounceKid)
        {
            damageInfo = instance_create(x + (sprite_width / 2 - sprite_xoffset) * image_xscale,y + (sprite_height / 2 - sprite_yoffset) * image_yscale,obj_damageInfo);
            damageInfo.damage = other.dmg;
            
            if(global.monsterSuperCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 2.25);
                damageInfo.font = fontDamageInfo3;
                damageInfo.color = c_red;        
                damageInfo.level = 3;
            }
            else if(global.monsterCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 1.5);
                damageInfo.font = fontDamageInfo2;
                damageInfo.color = c_yellow;        
                damageInfo.level = 2;
            }
            else
            {
                //damageInfo.damage = global.defenseDamage;
                damageInfo.font = fontDamageInfo1;
                damageInfo.color = c_white;        
                damageInfo.level = 1;
            }
        }
    }
    if(global.hp &lt;= 0 &amp;&amp; !global.GalleryBossFight)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(obj_phase4BounceKid)
        {
            killPlayer();
            instance_destroy();
        }
    }
    else if(global.GalleryBossFight &amp;&amp; global.GalleryBossFightHp &lt;= 0)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(obj_phase4BounceKid)
        {
            killPlayer();
            instance_destroy();
        }
    }
    else
    {
        global.cooldown = true;
        if(global.Blockable)
        {
            if(global.isAddMsg)
            {
                msg = "Kid block the damage.";
            }
            global.Blockable = false;
            sound_fix(sndgras);
            with(instance_create(obj_phase4BounceKid.x, obj_phase4BounceKid.y, obj_blockBubble))
            {
                target = obj_phase4BounceKid;
            }
            global.monsterCritical = false;
            global.monsterSuperCritical = false;
            /*if(object_index != global.combatObj &amp;&amp; !isStable)
            {                
                instance_destroy();
            }*/
        }
        else
        {
            if(sndMagic)
            {
                sound_fix(sndWind2);
                with(instance_create(x,y,obj_magicBlock))
                {
                    target = obj_phase4BounceKid;
                }
            }
            else
            {
                sound_fix(sndKidHit);
                obj_playerHitController.alarm[8] = 1;
            }
        }    
        obj_playerHitController.alarm[9] = 50;
    }
    if(global.isAddMsg)
    {
        with(global.msgController)
        {
            msg = other.msg;
            event_user(0);
        }
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="4" ename="obj_shiliuPlane">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>if(instance_exists(obj_tlRngBlackOut) || instance_exists(obj_tlRngBlackIn)) exit;

if(!global.cooldown &amp;&amp; !global.godmod)
{   
    sndMagic = false;
    //hit = true;
    if(global.defenseDamage &lt; 1)
    {
        global.defenseDamage = 1;
    }
    if(global.GalleryBossFight)
    {
        global.monsterSuperCritical = false;
        global.monsterCritical = false;
    }
    if(!global.Blockable)
    {
        if(global.monsterSuperCritical)
        {
            dmg = round(global.defenseDamage * 2.25);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak &amp;&amp; global.MagicHP &gt; 0)
                    {                    
                        if(global.MagicHP &gt;= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
            global.monsterSuperCritical = false;
        }
        else if(global.monsterCritical)
        {
            dmg = round(global.defenseDamage * 1.5);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak &amp;&amp; global.MagicHP &gt; 0)
                    {                    
                        if(global.MagicHP &gt;= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
            global.monsterCritical = false;
        }
        else
        {
            dmg = global.defenseDamage;
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= global.defenseDamage;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak &amp;&amp; global.MagicHP &gt; 0)
                    {                    
                        if(global.MagicHP &gt;= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "Kid got " + string(dmg) + " damage.";
            }
        }  
        with(obj_shiliuPlane)
        {
            damageInfo = instance_create(x + (sprite_width / 2 - sprite_xoffset) * image_xscale,y + (sprite_height / 2 - sprite_yoffset) * image_yscale,obj_damageInfo);
            damageInfo.damage = other.dmg;
            
            if(global.monsterSuperCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 2.25);
                damageInfo.font = fontDamageInfo3;
                damageInfo.color = c_red;        
                damageInfo.level = 3;
            }
            else if(global.monsterCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 1.5);
                damageInfo.font = fontDamageInfo2;
                damageInfo.color = c_yellow;        
                damageInfo.level = 2;
            }
            else
            {
                //damageInfo.damage = global.defenseDamage;
                damageInfo.font = fontDamageInfo1;
                damageInfo.color = c_white;        
                damageInfo.level = 1;
            }
        }
    }
    if(global.hp &lt;= 0 &amp;&amp; !global.GalleryBossFight)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(obj_shiliuPlane)
        {
            killPlayer();
            instance_destroy();
        }
    }
    else if(global.GalleryBossFight &amp;&amp; global.GalleryBossFightHp &lt;= 0)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(obj_shiliuPlane)
        {
            killPlayer();
            instance_destroy();
        }
    }
    else
    {
        global.cooldown = true;
        if(global.Blockable)
        {
            if(global.isAddMsg)
            {
                msg = "Kid block the damage.";
            }
            global.Blockable = false;
            sound_fix(sndgras);
            with(instance_create(obj_shiliuPlane.x, obj_shiliuPlane.y, obj_blockBubble))
            {
                target = obj_shiliuPlane;
            }
            global.monsterCritical = false;
            global.monsterSuperCritical = false;
            /*if(object_index != global.combatObj &amp;&amp; !isStable)
            {                
                instance_destroy();
            }*/
        }
        else
        {
            if(sndMagic)
            {
                sound_fix(sndWind2);
                with(instance_create(x,y,obj_magicBlock))
                {
                    target = obj_shiliuPlane;
                }
            }
            else
            {
                sound_fix(sndKidHit);
                obj_playerHitController.alarm[8] = 1;
            }
        }    
        obj_playerHitController.alarm[9] = 50;
    }
    if(global.isAddMsg)
    {
        with(global.msgController)
        {
            msg = other.msg;
            event_user(0);
        }
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="4" ename="player2">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>if(!global.cooldown &amp;&amp; !global.godmod)
{   
    sndMagic = false;
    //hit = true;
    if(global.defenseDamage &lt; 1)
    {
        global.defenseDamage = 1;
    }
    if(global.GalleryBossFight)
    {
        global.monsterSuperCritical = false;
        global.monsterCritical = false;
    }
    if(!global.Blockable)
    {
        if(global.monsterSuperCritical)
        {
            dmg = round(global.defenseDamage * 2.25);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak &amp;&amp; global.MagicHP &gt; 0)
                    {                    
                        if(global.MagicHP &gt;= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
            //global.monsterSuperCritical = false;
        }
        else if(global.monsterCritical)
        {
            dmg = round(global.defenseDamage * 1.5);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak &amp;&amp; global.MagicHP &gt; 0)
                    {                    
                        if(global.MagicHP &gt;= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
            //global.monsterCritical = false;
        }
        else
        {
            dmg = global.defenseDamage;
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= global.defenseDamage;
            }
            else
            { 
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak &amp;&amp; global.MagicHP &gt; 0)
                    {                    
                        if(global.MagicHP &gt;= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "Kid got " + string(dmg) + " damage.";
            }
        }  
        with(player2)
        {
            damageInfo = instance_create(x + (sprite_width / 2 - sprite_xoffset) * image_xscale,y + (sprite_height / 2 - sprite_yoffset) * image_yscale,obj_damageInfo);
            damageInfo.damage = other.dmg;
            
            if(global.monsterSuperCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 2.25);
                damageInfo.font = fontDamageInfo3;
                damageInfo.color = c_red;        
                damageInfo.level = 3;
            }
            else if(global.monsterCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 1.5);
                damageInfo.font = fontDamageInfo2;
                damageInfo.color = c_yellow;        
                damageInfo.level = 2;
            }
            else
            {
                //damageInfo.damage = global.defenseDamage;
                damageInfo.font = fontDamageInfo1;
                damageInfo.color = c_white;        
                damageInfo.level = 1;
            }
        }
        global.monsterSuperCritical = false;
        global.monsterCritical = false;
    }
    if(global.hp &lt;= 0 &amp;&amp; !global.GalleryBossFight)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(player2)
        {
            killPlayer();
        }
    }
    else if(global.GalleryBossFight &amp;&amp; global.GalleryBossFightHp &lt;= 0)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(player2)
        {
            killPlayer();
        }
    }
    else
    {
        global.cooldown = true;
        if(global.Blockable)
        {
            if(global.isAddMsg)
            {
                msg = "Kid block the damage.";
            }
            global.Blockable = false;
            sound_fix(sndgras);
            instance_create(player2.x, player2.y, obj_blockBubble);
            global.monsterCritical = false;
            global.monsterSuperCritical = false;
            if(object_index != global.combatObj &amp;&amp; !isStable)
            {                
                instance_destroy();
            }
        }
        else
        {
            if(sndMagic)
            {
                sound_fix(sndWind2);
                with(instance_create(x,y,obj_magicBlock))
                {
                    target = player2;
                }
            }
            else
            {
                sound_fix(sndKidHit);
                obj_playerHitController.alarm[8] = 1;
            }
        }    
        obj_playerHitController.alarm[9] = 50;
    }
    if(global.isAddMsg)
    {
        with(global.msgController)
        {
            msg = other.msg;
            event_user(0);
        }
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="4" ename="player">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>if(room == rTaisa) exit;
if(instance_exists(obj_tlRngBlackOut) || instance_exists(obj_tlRngBlackIn)) exit;

if(!global.cooldown &amp;&amp; !global.godmod)
{   
    sndMagic = false;
    //hit = true;
    if(global.defenseDamage &lt; 1)
    {
        global.defenseDamage = 1;
    }
    if(global.GalleryBossFight)
    {
        global.monsterSuperCritical = false;
        global.monsterCritical = false;
    }
    if(!global.Blockable)
    {
        if(global.monsterSuperCritical)
        {
            dmg = round(global.defenseDamage * 2.25);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak &amp;&amp; global.MagicHP &gt; 0)
                    {                    
                        if(global.MagicHP &gt;= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
        }
        else if(global.monsterCritical)
        {
            dmg = round(global.defenseDamage * 1.5);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak &amp;&amp; global.MagicHP &gt; 0)
                    {                    
                        if(global.MagicHP &gt;= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
        }
        else
        {
            dmg = global.defenseDamage;
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= global.defenseDamage;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak &amp;&amp; global.MagicHP &gt; 0)
                    {                    
                        if(global.MagicHP &gt;= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "Kid got " + string(dmg) + " damage.";
            }
        }  
        with(player)
        {
            damageInfo = instance_create(x + (sprite_width / 2 - sprite_xoffset) * image_xscale,y + (sprite_height / 2 - sprite_yoffset) * image_yscale,obj_damageInfo);
            damageInfo.damage = other.dmg;
            
            if(global.monsterSuperCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 2.25);
                damageInfo.font = fontDamageInfo3;
                damageInfo.color = c_red;        
                damageInfo.level = 3;
            }
            else if(global.monsterCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 1.5);
                damageInfo.font = fontDamageInfo2;
                damageInfo.color = c_yellow;        
                damageInfo.level = 2;
            }
            else
            {
                //damageInfo.damage = global.defenseDamage;
                damageInfo.font = fontDamageInfo1;
                damageInfo.color = c_white;        
                damageInfo.level = 1;
            }
        }
        global.monsterSuperCritical = false;
        global.monsterCritical = false;
    }
    if(global.hp &lt;= 0 &amp;&amp; !global.GalleryBossFight)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(player)
        {
            killPlayer();
        }
    }
    else if(global.GalleryBossFight &amp;&amp; global.GalleryBossFightHp &lt;= 0)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(player)
        {
            killPlayer();
        }
    }
    else
    {
        global.cooldown = true;
        if(global.Blockable)
        {
            if(global.isAddMsg)
            {
                msg = "Kid block the damage.";
            }
            global.Blockable = false;
            sound_fix(sndgras);
            instance_create(player.x, player.y, obj_blockBubble);
            global.monsterCritical = false;
            global.monsterSuperCritical = false;
            /*if(object_index != global.combatObj &amp;&amp; !isStable)
            {                
                instance_destroy();
            }*/
        }
        else
        {
            if(sndMagic)
            {
                sound_fix(sndWind2);
                with(instance_create(x,y,obj_magicBlock))
                {
                    target = player;
                }
            }
            else
            {
                sound_fix(sndKidHit);
                obj_playerHitController.alarm[8] = 1;
            }
            
        }    
        obj_playerHitController.alarm[9] = 50;
    }
    if(global.isAddMsg)
    {
        with(global.msgController)
        {
            msg = other.msg;
            event_user(0);
        }
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </events>
  <PhysicsObject>0</PhysicsObject>
  <PhysicsObjectSensor>0</PhysicsObjectSensor>
  <PhysicsObjectShape>0</PhysicsObjectShape>
  <PhysicsObjectDensity>0.5</PhysicsObjectDensity>
  <PhysicsObjectRestitution>0.100000001490116</PhysicsObjectRestitution>
  <PhysicsObjectGroup>0</PhysicsObjectGroup>
  <PhysicsObjectLinearDamping>0.100000001490116</PhysicsObjectLinearDamping>
  <PhysicsObjectAngularDamping>0.100000001490116</PhysicsObjectAngularDamping>
  <PhysicsObjectFriction>0.200000002980232</PhysicsObjectFriction>
  <PhysicsObjectAwake>-1</PhysicsObjectAwake>
  <PhysicsObjectKinematic>0</PhysicsObjectKinematic>
  <PhysicsShapePoints/>
</object>
