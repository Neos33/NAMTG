// 2025-06-19 20:05:40
#event properties (no comments/etc. here are saved)
parent_index = -1;
uses_physics = false;

#event create
hit = false;
hp = 0;
sndMagic = false;
dmg = 0;
tempDmp = 0;
msg = "";
cooldown = 0;



#section Added (some common variables used on their child objects)
status = 0;
phase = 0;
time = 0;
dir = 0;
dis = 0;
targetX = 0;
targetY = 0;
spd = 0;
type = 0;
start = false;

angle = 0;
alpha = 0;

#event alarm8
if(global.cooldown)
{
    with(player)
    {
        if(image_alpha == 0)
        {
            image_alpha = 1;
        }
        else 
        {
            image_alpha = 0;
        }
    }
    with(player2)
    {
        if(image_alpha == 0)
        {
            image_alpha = 1;
        }
        else 
        {
            image_alpha = 0;
        }
    }
    alarm[8] = 5;
}

#event alarm9
player.image_alpha = 1;
player2.image_alpha = 1;
global.cooldown = false;
hit = false;

#event collision:player
if(room == rTaisa) exit;
if(instance_exists(obj_tlRngBlackOut) || instance_exists(obj_tlRngBlackIn)) exit;

if(!global.cooldown && !global.godmod)
{   
    sndMagic = false;
    //hit = true;
    if(global.defenseDamage < 1)
    {
        global.defenseDamage = 1;
    }
    if(global.GalleryBossFight)
    {
        global.monsterSuperCritical = false;
        global.monsterCritical = false;
    }
    if(!global.Blockable)
    {
        if(global.monsterSuperCritical)
        {
            dmg = round(global.defenseDamage * 2.25);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak && global.MagicHP > 0)
                    {                    
                        if(global.MagicHP >= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
        }
        else if(global.monsterCritical)
        {
            dmg = round(global.defenseDamage * 1.5);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak && global.MagicHP > 0)
                    {                    
                        if(global.MagicHP >= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
        }
        else
        {
            dmg = global.defenseDamage;
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= global.defenseDamage;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak && global.MagicHP > 0)
                    {                    
                        if(global.MagicHP >= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "Kid got " + string(dmg) + " damage.";
            }
        }  
        with(player)
        {
            damageInfo = instance_create(x + (sprite_width / 2 - sprite_xoffset) * image_xscale,y + (sprite_height / 2 - sprite_yoffset) * image_yscale,obj_damageInfo);
            damageInfo.damage = other.dmg;
            
            if(global.monsterSuperCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 2.25);
                damageInfo.font = fontDamageInfo3;
                damageInfo.color = c_red;        
                damageInfo.level = 3;
            }
            else if(global.monsterCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 1.5);
                damageInfo.font = fontDamageInfo2;
                damageInfo.color = c_yellow;        
                damageInfo.level = 2;
            }
            else
            {
                //damageInfo.damage = global.defenseDamage;
                damageInfo.font = fontDamageInfo1;
                damageInfo.color = c_white;        
                damageInfo.level = 1;
            }
        }
        global.monsterSuperCritical = false;
        global.monsterCritical = false;
    }
    if(global.hp <= 0 && !global.GalleryBossFight)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(player)
        {
            killPlayer();
        }
    }
    else if(global.GalleryBossFight && global.GalleryBossFightHp <= 0)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(player)
        {
            killPlayer();
        }
    }
    else
    {
        global.cooldown = true;
        if(global.Blockable)
        {
            if(global.isAddMsg)
            {
                msg = "Kid block the damage.";
            }
            global.Blockable = false;
            sound_fix(sndgras);
            instance_create(player.x, player.y, obj_blockBubble);
            global.monsterCritical = false;
            global.monsterSuperCritical = false;
            /*if(object_index != global.combatObj && !isStable)
            {                
                instance_destroy();
            }*/
        }
        else
        {
            if(sndMagic)
            {
                sound_fix(sndWind2);
                with(instance_create(x,y,obj_magicBlock))
                {
                    target = player;
                }
            }
            else
            {
                sound_fix(sndKidHit);
                obj_playerHitController.alarm[8] = 1;
            }
            
        }    
        obj_playerHitController.alarm[9] = 50;
    }
    if(global.isAddMsg)
    {
        with(global.msgController)
        {
            msg = other.msg;
            event_user(0);
        }
    }
}

#event collision:player2
if(!global.cooldown && !global.godmod)
{   
    sndMagic = false;
    //hit = true;
    if(global.defenseDamage < 1)
    {
        global.defenseDamage = 1;
    }
    if(global.GalleryBossFight)
    {
        global.monsterSuperCritical = false;
        global.monsterCritical = false;
    }
    if(!global.Blockable)
    {
        if(global.monsterSuperCritical)
        {
            dmg = round(global.defenseDamage * 2.25);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak && global.MagicHP > 0)
                    {                    
                        if(global.MagicHP >= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
            //global.monsterSuperCritical = false;
        }
        else if(global.monsterCritical)
        {
            dmg = round(global.defenseDamage * 1.5);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak && global.MagicHP > 0)
                    {                    
                        if(global.MagicHP >= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
            //global.monsterCritical = false;
        }
        else
        {
            dmg = global.defenseDamage;
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= global.defenseDamage;
            }
            else
            { 
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak && global.MagicHP > 0)
                    {                    
                        if(global.MagicHP >= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "Kid got " + string(dmg) + " damage.";
            }
        }  
        with(player2)
        {
            damageInfo = instance_create(x + (sprite_width / 2 - sprite_xoffset) * image_xscale,y + (sprite_height / 2 - sprite_yoffset) * image_yscale,obj_damageInfo);
            damageInfo.damage = other.dmg;
            
            if(global.monsterSuperCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 2.25);
                damageInfo.font = fontDamageInfo3;
                damageInfo.color = c_red;        
                damageInfo.level = 3;
            }
            else if(global.monsterCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 1.5);
                damageInfo.font = fontDamageInfo2;
                damageInfo.color = c_yellow;        
                damageInfo.level = 2;
            }
            else
            {
                //damageInfo.damage = global.defenseDamage;
                damageInfo.font = fontDamageInfo1;
                damageInfo.color = c_white;        
                damageInfo.level = 1;
            }
        }
        global.monsterSuperCritical = false;
        global.monsterCritical = false;
    }
    if(global.hp <= 0 && !global.GalleryBossFight)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(player2)
        {
            killPlayer();
        }
    }
    else if(global.GalleryBossFight && global.GalleryBossFightHp <= 0)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(player2)
        {
            killPlayer();
        }
    }
    else
    {
        global.cooldown = true;
        if(global.Blockable)
        {
            if(global.isAddMsg)
            {
                msg = "Kid block the damage.";
            }
            global.Blockable = false;
            sound_fix(sndgras);
            instance_create(player2.x, player2.y, obj_blockBubble);
            global.monsterCritical = false;
            global.monsterSuperCritical = false;
            if(object_index != global.combatObj && !isStable)
            {                
                instance_destroy();
            }
        }
        else
        {
            if(sndMagic)
            {
                sound_fix(sndWind2);
                with(instance_create(x,y,obj_magicBlock))
                {
                    target = player2;
                }
            }
            else
            {
                sound_fix(sndKidHit);
                obj_playerHitController.alarm[8] = 1;
            }
        }    
        obj_playerHitController.alarm[9] = 50;
    }
    if(global.isAddMsg)
    {
        with(global.msgController)
        {
            msg = other.msg;
            event_user(0);
        }
    }
}

#event collision:obj_shiliuPlane
if(instance_exists(obj_tlRngBlackOut) || instance_exists(obj_tlRngBlackIn)) exit;

if(!global.cooldown && !global.godmod)
{   
    sndMagic = false;
    //hit = true;
    if(global.defenseDamage < 1)
    {
        global.defenseDamage = 1;
    }
    if(global.GalleryBossFight)
    {
        global.monsterSuperCritical = false;
        global.monsterCritical = false;
    }
    if(!global.Blockable)
    {
        if(global.monsterSuperCritical)
        {
            dmg = round(global.defenseDamage * 2.25);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak && global.MagicHP > 0)
                    {                    
                        if(global.MagicHP >= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
            global.monsterSuperCritical = false;
        }
        else if(global.monsterCritical)
        {
            dmg = round(global.defenseDamage * 1.5);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak && global.MagicHP > 0)
                    {                    
                        if(global.MagicHP >= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
            global.monsterCritical = false;
        }
        else
        {
            dmg = global.defenseDamage;
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= global.defenseDamage;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak && global.MagicHP > 0)
                    {                    
                        if(global.MagicHP >= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "Kid got " + string(dmg) + " damage.";
            }
        }  
        with(obj_shiliuPlane)
        {
            damageInfo = instance_create(x + (sprite_width / 2 - sprite_xoffset) * image_xscale,y + (sprite_height / 2 - sprite_yoffset) * image_yscale,obj_damageInfo);
            damageInfo.damage = other.dmg;
            
            if(global.monsterSuperCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 2.25);
                damageInfo.font = fontDamageInfo3;
                damageInfo.color = c_red;        
                damageInfo.level = 3;
            }
            else if(global.monsterCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 1.5);
                damageInfo.font = fontDamageInfo2;
                damageInfo.color = c_yellow;        
                damageInfo.level = 2;
            }
            else
            {
                //damageInfo.damage = global.defenseDamage;
                damageInfo.font = fontDamageInfo1;
                damageInfo.color = c_white;        
                damageInfo.level = 1;
            }
        }
    }
    if(global.hp <= 0 && !global.GalleryBossFight)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(obj_shiliuPlane)
        {
            killPlayer();
            instance_destroy();
        }
    }
    else if(global.GalleryBossFight && global.GalleryBossFightHp <= 0)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(obj_shiliuPlane)
        {
            killPlayer();
            instance_destroy();
        }
    }
    else
    {
        global.cooldown = true;
        if(global.Blockable)
        {
            if(global.isAddMsg)
            {
                msg = "Kid block the damage.";
            }
            global.Blockable = false;
            sound_fix(sndgras);
            with(instance_create(obj_shiliuPlane.x, obj_shiliuPlane.y, obj_blockBubble))
            {
                target = obj_shiliuPlane;
            }
            global.monsterCritical = false;
            global.monsterSuperCritical = false;
            /*if(object_index != global.combatObj && !isStable)
            {                
                instance_destroy();
            }*/
        }
        else
        {
            if(sndMagic)
            {
                sound_fix(sndWind2);
                with(instance_create(x,y,obj_magicBlock))
                {
                    target = obj_shiliuPlane;
                }
            }
            else
            {
                sound_fix(sndKidHit);
                obj_playerHitController.alarm[8] = 1;
            }
        }    
        obj_playerHitController.alarm[9] = 50;
    }
    if(global.isAddMsg)
    {
        with(global.msgController)
        {
            msg = other.msg;
            event_user(0);
        }
    }
}

#event collision:obj_phase4BounceKid
if(instance_exists(obj_tlRngBlackOut) || instance_exists(obj_tlRngBlackIn)) exit;

if(!global.cooldown && !global.godmod)
{   
    sndMagic = false;
    //hit = true;
    if(global.defenseDamage < 1)
    {
        global.defenseDamage = 1;
    }
    if(global.GalleryBossFight)
    {
        global.monsterSuperCritical = false;
        global.monsterCritical = false;
    }
    if(!global.Blockable)
    {
        if(global.monsterSuperCritical)
        {
            dmg = round(global.defenseDamage * 2.25);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak && global.MagicHP > 0)
                    {                    
                        if(global.MagicHP >= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
            global.monsterSuperCritical = false;
        }
        else if(global.monsterCritical)
        {
            dmg = round(global.defenseDamage * 1.5);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak && global.MagicHP > 0)
                    {                    
                        if(global.MagicHP >= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
            global.monsterCritical = false;
        }
        else
        {
            dmg = global.defenseDamage;
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= global.defenseDamage;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak && global.MagicHP > 0)
                    {                    
                        if(global.MagicHP >= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "Kid got " + string(dmg) + " damage.";
            }
        }  
        with(obj_phase4BounceKid)
        {
            damageInfo = instance_create(x + (sprite_width / 2 - sprite_xoffset) * image_xscale,y + (sprite_height / 2 - sprite_yoffset) * image_yscale,obj_damageInfo);
            damageInfo.damage = other.dmg;
            
            if(global.monsterSuperCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 2.25);
                damageInfo.font = fontDamageInfo3;
                damageInfo.color = c_red;        
                damageInfo.level = 3;
            }
            else if(global.monsterCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 1.5);
                damageInfo.font = fontDamageInfo2;
                damageInfo.color = c_yellow;        
                damageInfo.level = 2;
            }
            else
            {
                //damageInfo.damage = global.defenseDamage;
                damageInfo.font = fontDamageInfo1;
                damageInfo.color = c_white;        
                damageInfo.level = 1;
            }
        }
    }
    if(global.hp <= 0 && !global.GalleryBossFight)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(obj_phase4BounceKid)
        {
            killPlayer();
            instance_destroy();
        }
    }
    else if(global.GalleryBossFight && global.GalleryBossFightHp <= 0)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(obj_phase4BounceKid)
        {
            killPlayer();
            instance_destroy();
        }
    }
    else
    {
        global.cooldown = true;
        if(global.Blockable)
        {
            if(global.isAddMsg)
            {
                msg = "Kid block the damage.";
            }
            global.Blockable = false;
            sound_fix(sndgras);
            with(instance_create(obj_phase4BounceKid.x, obj_phase4BounceKid.y, obj_blockBubble))
            {
                target = obj_phase4BounceKid;
            }
            global.monsterCritical = false;
            global.monsterSuperCritical = false;
            /*if(object_index != global.combatObj && !isStable)
            {                
                instance_destroy();
            }*/
        }
        else
        {
            if(sndMagic)
            {
                sound_fix(sndWind2);
                with(instance_create(x,y,obj_magicBlock))
                {
                    target = obj_phase4BounceKid;
                }
            }
            else
            {
                sound_fix(sndKidHit);
                obj_playerHitController.alarm[8] = 1;
            }
        }    
        obj_playerHitController.alarm[9] = 50;
    }
    if(global.isAddMsg)
    {
        with(global.msgController)
        {
            msg = other.msg;
            event_user(0);
        }
    }
}

#event collision:obj_playerDotMask
if(instance_exists(obj_tlRngBlackOut) || instance_exists(obj_tlRngBlackIn)) exit;

if(!global.cooldown && !global.godmod)
{   
    sndMagic = false;
    //hit = true;
    if(global.defenseDamage < 1)
    {
        global.defenseDamage = 1;
    }
    if(global.GalleryBossFight)
    {
        global.monsterSuperCritical = false;
        global.monsterCritical = false;
    }
    if(!global.Blockable)
    {
        if(global.monsterSuperCritical)
        {
            dmg = round(global.defenseDamage * 2.25);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak && global.MagicHP > 0)
                    {                    
                        if(global.MagicHP >= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
            global.monsterSuperCritical = false;
        }
        else if(global.monsterCritical)
        {
            dmg = round(global.defenseDamage * 1.5);
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= dmg;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak && global.MagicHP > 0)
                    {                    
                        if(global.MagicHP >= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "The monster did a critical hit! Kid got " + string(dmg) + " damage.";
            }
            global.monsterCritical = false;
        }
        else
        {
            dmg = global.defenseDamage;
            if(global.GalleryBossFight)
            {
                global.GalleryBossFightHp -= global.defenseDamage;
            }
            else
            {
                if(!global.inGallery)
                {
                    if(global.SaintShield)
                    {
                        tempDmp = ceil(dmg/2);
                        dmg = tempDmp;
                    }   
                    if(global.MagicCloak && global.MagicHP > 0)
                    {                    
                        if(global.MagicHP >= dmg)
                        {
                            sndMagic = true;
                            global.MagicHP -= dmg;
                            dmg = 0;
                        }
                        else
                        {
                            dmg -= global.MagicHP;
                            global.MagicHP = 0;
                        }
                    }
                }
                global.hp -= dmg;
            }
            if(global.isAddMsg)
            {
                msg = "Kid got " + string(dmg) + " damage.";
            }
        }  
        with(player)
        {
            damageInfo = instance_create(x + (sprite_width / 2 - sprite_xoffset) * image_xscale,y + (sprite_height / 2 - sprite_yoffset) * image_yscale,obj_damageInfo);
            damageInfo.damage = other.dmg;
            
            if(global.monsterSuperCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 2.25);
                damageInfo.font = fontDamageInfo3;
                damageInfo.color = c_red;        
                damageInfo.level = 3;
            }
            else if(global.monsterCritical)
            {
                //damageInfo.damage = round(global.defenseDamage * 1.5);
                damageInfo.font = fontDamageInfo2;
                damageInfo.color = c_yellow;        
                damageInfo.level = 2;
            }
            else
            {
                //damageInfo.damage = global.defenseDamage;
                damageInfo.font = fontDamageInfo1;
                damageInfo.color = c_white;        
                damageInfo.level = 1;
            }
        }
    }
    if(global.hp <= 0 && !global.GalleryBossFight)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(player)
        {
            killPlayer();
        }
    }
    else if(global.GalleryBossFight && global.GalleryBossFightHp <= 0)
    {
        if(global.isAddMsg)
        {
            msg += " Kid died.";
        }
        with(player)
        {
            killPlayer();
        }
    }
    else
    {
        global.cooldown = true;
        if(global.Blockable)
        {
            if(global.isAddMsg)
            {
                msg = "Kid block the damage.";
            }
            global.Blockable = false;
            sound_fix(sndgras);
            instance_create(player.x, player.y, obj_blockBubble);
            global.monsterCritical = false;
            global.monsterSuperCritical = false;
            /*if(object_index != global.combatObj && !isStable)
            {                
                instance_destroy();
            }*/
        }
        else
        {
            if(sndMagic)
            {
                sound_fix(sndWind2);
                with(instance_create(x,y,obj_magicBlock))
                {
                    target = player;
                }
            }
            else
            {
                sound_fix(sndKidHit);
                obj_playerHitController.alarm[8] = 1;
            }
        }    
        obj_playerHitController.alarm[9] = 50;
    }
    if(global.isAddMsg)
    {
        with(global.msgController)
        {
            msg = other.msg;
            event_user(0);
        }
    }
}