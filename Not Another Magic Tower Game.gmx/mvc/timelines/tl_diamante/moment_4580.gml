with(obj_diaDanmaku2)
{
    if(cc=1)
    {
        vspeed=4.7;
        friction=cal_friction(vspeed,180);
    }
    if(cc=2)
    {
        vspeed=-4.7;
        friction=cal_friction(vspeed,180);
    }
}

