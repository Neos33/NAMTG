isVisible = random(100);
if(isVisible > 90)
{
    visible = false;
}

isChange = random(100);
if(isChange > 30)
{
    alarm[0] = 5;
}
image_alpha = 1;
image_speed = 0;
image_index = irandom_range(0,4);

