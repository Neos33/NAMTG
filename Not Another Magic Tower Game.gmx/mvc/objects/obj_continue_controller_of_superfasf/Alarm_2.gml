/// @description  Going to the player (Start)
/// @param Start
with(obj_superFasf_combat)
{
    image_blend=c_red;
    image_speed=2;
    sound_fix(snd_riioooooooo);
    move_towards_point(player.x,player.y,8)
}
pp=true;
//alarm[3]=50;

