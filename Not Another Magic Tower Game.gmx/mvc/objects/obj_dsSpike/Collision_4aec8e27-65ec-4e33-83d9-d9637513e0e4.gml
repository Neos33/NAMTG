/*if(stop)
    exit;
other.x = x;
if(instance_exists(target))
{
    other.trace = true;
    other.target = target;
    with(other)
    {
        alarm[0] = 100;
    }
    //other.color = color;
}
else
{
    other.direction = 90;
    other.image_angle = 90;
}

/* */
/*  */
